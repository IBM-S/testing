feature extracion.py
se ocupa de la siguiente forma:
python3 feature_extraction.py -d {carpeta donde se encuentran las instancias en el formato vrp lib}
ej: python3 feature_extraction.py -d vrp_lib_mdvrp/

codigo_latex.py
lo cree para poder hacer mas facil colocar las imagenes en el latex, para el caso X-nA-kB, ya que
son 100

feature_extraction.py
calcula las features de las instancias pXX y prXX

ver_capacidad.py
crea un txt leyendo el tipo de instnacia y saca el total, minimo, maximo, promedio, desviacion 
estandar de las demandas

transformar_instance_to_vrplib.py
toma el formato que estan las instancias pXX y prXX, y las transforma al formato vrplib, esto para
poder leer mas facilmente las instancias en el codigo feature_extraction.py

ver.py
crea una imagen en la carpeta images el cual es el resultado del final del algoritmo genetico, la
informacion la lee desde el archivo que se crea que es output.js

eliminar_1er_num.py
toma las instancias prXX y les quita el numero extra que tenian y el nuevo nombre que tienen 
es nuevo_prXX

_features_{instancia}.csv
ahi se guardan los resultados de las features encontradas

carpeta solutions
aqui se gurda la solucion final del algoritmo genetico, el csoto
las rutas ...
